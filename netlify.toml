[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback is handled by public/_redirects, which Vite copies into the
# publish directory. Deploy Previews for pull requests are enabled by default
# once the repo is connected to Netlify.
