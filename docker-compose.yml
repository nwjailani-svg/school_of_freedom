services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: school-of-freedom:latest
    container_name: school-of-freedom
    ports:
      - "8080:80"
    restart: unless-stopped
